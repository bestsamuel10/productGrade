using Microsoft.Extensions.DependencyInjection;

namespace ProductionGrade.Extensions
{
    public static class SwaggerConfig
    {
        /// <summary>
        /// Extension method placeholder for Swagger configuration.
        /// Currently simplified because Swagger setup is handled in Program.cs.
        /// </summary>
        public static void AddSwaggerDocumentation(this IServiceCollection services)
        {
            // Swagger configuration is centralized in Program.cs.
            // This method is left here for future customizations if needed.
        }
    }
}
